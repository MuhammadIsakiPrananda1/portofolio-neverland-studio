APP_NAME="Neverland Studio API"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=neverland_portfolio
DB_USERNAME=neverland
DB_PASSWORD=password
DB_ROOT_PASSWORD=rootpassword

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# WebSocket / Broadcasting Configuration (Using Pusher Protocol)
PUSHER_APP_ID=local-app-id
PUSHER_APP_KEY=local-app-key
PUSHER_APP_SECRET=local-app-secret
PUSHER_HOST=127.0.0.1
PUSHER_PORT=6001
PUSHER_SCHEME=http
PUSHER_APP_CLUSTER=mt1

# Alternative: Using Laravel Reverb (Recommended for production)
# REVERB_APP_ID=
# REVERB_APP_KEY=
# REVERB_APP_SECRET=
# REVERB_HOST=localhost
# REVERB_PORT=8080
# REVERB_SCHEME=http

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="hello@neverlandstudio.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# CORS Configuration
CORS_ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000,http://localhost:8001,http://localhost:80,http://portfolio:80,http://127.0.0.1:5173,http://127.0.0.1:3000"
CORS_ALLOWED_METHODS="*"
CORS_ALLOWED_HEADERS="*"
CORS_EXPOSED_HEADERS="Authorization,Content-Type,X-Requested-With,Content-Disposition"
CORS_MAX_AGE=86400
CORS_SUPPORTS_CREDENTIALS=true

# Sanctum Configuration
SANCTUM_STATEFUL_DOMAINS="localhost:5173,localhost:3000,localhost:8001,localhost:80,portfolio:80,127.0.0.1:5173,127.0.0.1:3000"

# API Configuration
API_PREFIX=api
API_VERSION=v1
API_RATE_LIMIT=60

# Frontend URL
FRONTEND_URL=http://localhost:5173

# VPS / VM Configuration
VPS_IP=your.vps.ip.address

# OpenVPN Configuration (for VM Playground VPN access)
# VPN_IP_POOL: comma-separated list of VPN server IPs that rotate every hour
# Leave empty to use VPS_IP as the sole server
VPN_IP_POOL=
VPN_PORT=1194
VPN_PROTO=udp
# Paste the full PEM-encoded CA certificate (single line or multi-line) here
VPN_CA_CERT=
# Paste the tls-auth / tls-crypt static key here
VPN_TLS_AUTH_KEY=
